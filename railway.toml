[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# /health answers 503 until the Whisper model is loaded; first boot downloads it.
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
